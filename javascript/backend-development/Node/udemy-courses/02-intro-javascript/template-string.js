const proName = 'Deadpool';
const real = 'Wade Winston';

const normal = proName + ' ' + real;
const template = `${proName} ${real}`;

console.log(normal);
console.log(template);

const html = `
    <h1>Hola</h1>
    <p>Mundo</p>
`;

console.log(html);